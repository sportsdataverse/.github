# Shown as the "Sponsor" button on every sportsdataverse repo that has no FUNDING.yml of its own.
ko_fi: G2G0KJ588
# github: sportsdataverse      # enable once the org's GitHub Sponsors listing is published
# patreon: <slug>              # fill in the Patreon page slug
